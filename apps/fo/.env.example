# Optional legacy /admin redirect. Admin service origin only, without /admin.
# Local development defaults to http://127.0.0.1:5175.
# Production defaults to https://admin.aiqaver.com.
# Override at build time only when deploying to a different Admin domain.
# VITE_ADMIN_URL=https://admin.aiqaver.com
