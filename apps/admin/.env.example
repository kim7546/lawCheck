# Railway Admin Variables reference. See docs/railway-variables.md.
# Replace example domains with services in the SAME Railway environment.
# Set runtime variables in Railway; preview does not load this file.
NODE_ENV=production
PORT=4175
# Public API domain. For a separate development API, use that environment's origin.
API_PROXY_TARGET=https://api.aiqaver.com
# Browser-visible FO link; rebuild after changing. Production: https://aiqaver.com
VITE_FO_URL=https://your-fo-service.example.com
# Optional extra custom hostnames (no scheme or path).
# admin.aiqaver.com and lawcheckadmin-production.up.railway.app are already allowed.
PREVIEW_ALLOWED_HOSTS=
# Railway supplies RAILWAY_PUBLIC_DOMAIN after a public domain is configured.
# ADMIN_EMAIL, DATABASE_URL and OPENAI_API_KEY belong on the API service.
